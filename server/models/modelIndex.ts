import CompanyInfo from './companyInfo.model';
import Role from './role.model';
import User from './user.model';

const models = {
  CompanyInfo,
  Role,
  User,
};

export default models;
export { CompanyInfo, Role, User }; 
